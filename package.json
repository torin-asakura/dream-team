{
  "private": true,
  "scripts": {
    "landing:build": "yarn workspace @landing/app build",
    "rezip": "rm -rf dt-next.zip && zip -r dt-next.zip dt-next",
    "prepare": "yarn landing:build && yarn rezip"
  },
  "resolutions": {
    "@types/node": "14.17.6"
  },
  "devDependencies": {
    "@atls/actl-cli": "^0.4.48",
    "@types/node": "14.17.6",
    "@types/react": "^17.0.15",
    "@types/react-dom": "^17.0.9"
  },
  "workspaces": [
    "frontend/**/*"
  ]
}
